 version: '3'
 services:
   # db:
   #   image: postgres
   webserver:
     image: acirulis/wd-webdev-auto
     volumes:
       - ./webroot:/var/www/html
     #command: service nginx start
     restart: on-failure
     ports:
       - "80:80"
       - "443:443"
       - "3306:3306"
     # depends_on:
     #   - db
